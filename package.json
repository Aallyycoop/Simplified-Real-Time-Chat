{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "Simplified alternative to Slack chat",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "build-frontend": "npm ci && cd frontend && npm ci && npm run build",
    "start": "npx start-server -s ./frontend/build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd frontend && npm ci"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Aallyycoop/frontend-project-12.git"
  },
  "author": "Alexandra Dmitrieva",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^1.1.6"
  }
}
